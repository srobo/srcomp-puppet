from sr.comp.http import app

from werkzeug.wsgi import DispatcherMiddleware

app.config['COMPSTATE'] = '<%= @compstate_path %>'

app.wsgi_app = DispatcherMiddleware(app.wsgi_app, {
    '/comp-api': app.wsgi_app
})

bind = 'localhost:5112'
keepalive = 5
